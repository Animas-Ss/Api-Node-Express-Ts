export interface Car {
    color: string;
    gas: "gasoline" | "electric";
    year: number;
    description: string;
    price: number;
    name: String;
}
// como sabemos las interfaces son contratos que representan el tipo que va a ser nuestor objeto